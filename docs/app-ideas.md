# 後續應用開發候選

記錄於 bring-up 驗證完成後（2026-08-24，狀態於 2026-08-26 更新）。
每個應用啟動前仍須走 brainstorming → spec → plan 流程。

## 已完成應用（git 標籤保存）

| 應用 | 狀態 | 保存方式 |
| --- | --- | --- |
| 天氣看板 v1（Open-Meteo 四地點、30 分週期） | ✅ 完成 | 標籤 `weather-v1` |
| SD 相框 v1（`/raw_photos/*.raw`、翻頁＋輪播） | ✅ 完成 | 現行 master 韌體 |

## 待開發候選

### 1. 報價看板（下一個建議）

- 利用工作區既有台股 Shioaji 環境做每日收盤看板。
- 資料鏈路最長（本機 API 橋接或公開 API），建議在相框與天氣看板的
  電源/渲染/互動模式均已驗證後進行。
- 共同設計注意事項見下。

## 共同設計注意事項（來自實機量測）

- full refresh 實測約 4.4 s、partial 約 664 ms（選單類互動採整頁重繪
  較可靠，見 device-research.md）；更新節奏設計時以此為準。
- 每 5–30 次 partial 插入一次 full refresh（AGENTS.md）。
- 電池尚未接入；接入前須量測 SH1.0 極性並在 device-research.md 記錄方法。
- 應用間互斥（單一韌體槽位）：新應用開發時以標籤保存現行應用
  （慣例：`<app>-v1`），完成後合併回 master 取代。